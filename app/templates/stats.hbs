{{!-- {{debugger }} --}}

<Header @text="Stats list" />

<ul>
    {{#each @model as |stat|}}
    <Statistic @session_id="{{stat.id}}" @event_type={{stat.event_type}} @user_agent={{stat.user_agent}}
        @note={{stat.note}} @created_at={{stat.created_at}} @updated_at={{stat.updated_at}} @stats={{stat.stats}} />
    {{/each}}
</ul>