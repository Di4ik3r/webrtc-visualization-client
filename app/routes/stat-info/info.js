import Route from "@ember/routing/route";

export default class StatInfoInfoRoute extends Route {
  // model(params) {
  //   return this.store.findRecord("statistic", parseInt(params.statistic_id));
  // }
}
